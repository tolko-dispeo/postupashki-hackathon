# Mock data

Здесь будут лежать согласованные синтетические `placements.csv`, `events.csv`, `leads.csv`, `orders.csv` и `payments.csv`.

Данные должны описывать единую связную воронку, а не независимые случайные строки.
