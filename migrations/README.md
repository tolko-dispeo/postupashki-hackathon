# Миграции БД

Каждое изменение логической схемы оформляется новой миграцией в `versions/`.

Нельзя вручную менять уже применённую миграцию: это позволяет постепенно добавлять таблицы для Telegram, NLP, экспериментов и прогнозов без пересоздания базы.
